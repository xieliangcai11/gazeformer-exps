# GazeFormer 模型详解

> 本文档详细讲解本项目中用于 **3D 视线（gaze）估计** 的模型结构与实现原理，重点覆盖 `clipmodel.py`、`models.py`、`transformer_models.py` 三个文件。

---

## 目录

1. [项目概述](#1-项目概述)
2. [整体架构与数据流](#2-整体架构与数据流)
3. [clipmodel.py —— CLIP 基础模型](#3-clipmodelpy--clip-基础模型)
4. [models.py —— CLIP 语义融合特征提取](#4-modelspy--clip-语义融合特征提取)
5. [transformer_models.py —— MoE Transformer](#5-transformer_modelspy--moe-transformer)
6. [训练流程串联（train.py）](#6-训练流程串联trainpy)
7. [张量形状速查表](#7-张量形状速查表)
8. [关键设计思想总结](#8-关键设计思想总结)

---

## 1. 项目概述

**GazeFormer** 是一个基于 **CLIP 语义先验 + 混合专家（MoE）Transformer** 的 3D 视线估计框架。

核心思路：**视线方向不仅由眼睛决定，还受光照、头部姿态、背景等"上下文"影响**。因此模型：

1. 用冻结的 **CLIP** 把图像映射为语义向量，并用**文本提示（text prompt）** 描述光照/头姿/背景/视线方向等属性；
2. 通过**余弦相似度**自动选出与当前图像最匹配的属性文本向量，与图像特征相加，得到"上下文补偿特征"和"任务对齐特征"；
3. 再结合 **CNN** 提取的视觉几何特征与 **ViT 的 patch token**；
4. 把所有特征投影成 token 序列，送入 **MoE Transformer** 进行聚合，最终回归出 3D gaze 向量。

项目主要文件：

| 文件 | 作用 |
|------|------|
| `clipmodel.py` | OpenAI CLIP 的完整网络结构（视觉 + 文本双塔编码器） |
| `models.py` | 基于 CLIP 的语义特征融合模块（输出 feature_1/2/3） |
| `transformer_models.py` | MoE / Transformer 组件与顶层 `TransformerDeepSeek_gaze` |
| `train.py` | 训练/测试主循环，串联上述模块 |
| `config.py` | 数据集、CNN 骨干、CLIP 加载等全局配置 |

---

## 2. 整体架构与数据流

```
                 ┌────────────────────────────────────────────────────────────┐
                 │                       输入人脸图像                          │
                 └───────────────┬───────────────────────────┬────────────────┘
                                 │                           │
                    (CLIP 预处理) │                           │ (CNN 预处理)
                                 ▼                           ▼
                 ┌───────────────────────┐          ┌─────────────────────────┐
                 │  冻结的 CLIP 视觉编码器 │          │  CNN 骨干 (ResNet-50)   │
                 │  ViT-B/32 → img_feats  │          │  layer4 特征图           │
                 │        [B,512]         │          │  [B,2048,H,W]            │
                 └───────────┬───────────┘          └────────────┬────────────┘
                             │                                    │
                             │  与文本提示做余弦相似度，选出最匹配   │
                             │  的属性向量 (argmax)                 │
                             │                                    │
        ┌────────────────────┼────────────────────┐               │
        ▼                    ▼                    ▼               │
   feature_1           feature_2          token_img_patch        │
   img+光照/头姿/背景    img+视线方向标签    (ViT patch tokens)     │
   [B,512]             [B,512]            [B,N,768]              │
        │                    │                │                  │
        └──────────┬─────────┴────────────────┴──────────┬───────┘
                   │                                      │
                   ▼                                      ▼
   feature_3 = 展平 CNN 特征图 → [B, H*W, 2048]  ──────────┘
                   │
                   ▼
        ┌─────────────────────────────────────────┐
        │        TransformerDeepSeek_gaze          │
        │                                          │
        │  proj_f1: 512→768  → token_f1 [B,1,768]  │
        │  proj_f2: 512→768  → token_f2 [B,1,768]  │
        │  proj_f3: 2048→768 → token_f3 [B,N,768]  │
        │  proj_patch:768→768→ token_patch[B,N,768]│
        │                                          │
        │  拼接 CLS token + 全部 token               │
        │  经过 num_layers 层 BlockMoba(注意力+MoE) │
        │  取 CLS token → linear_head              │
        └───────────────────┬─────────────────────┘
                            ▼
                    3D gaze 向量 [B,3]
```

---

## 3. clipmodel.py —— CLIP 基础模型

该文件是 OpenAI CLIP 的官方实现（本项目通过 `clip.load("ViT-B/32")` 加载的就是它）。CLIP 是一个**双塔对比学习模型**：一个视觉编码器 + 一个文本编码器，把图像和文本分别映射到统一的嵌入空间（本项目为 512 维）。

### 3.1 主要组件

#### (1) `Bottleneck`
ResNet 的瓶颈残差块（1x1 降维 → 3x3 → 1x1 升维，通道扩 4 倍）。

与 torchvision 的差异：
- 下采样用 **avgpool** 而非 stride=2 卷积（抗混叠）；
- 所有卷积 stride 均为 1。

#### (2) `AttentionPool2d`
注意力池化。把 ResNet 最后的特征图压平成 token，再额外插入一个全局 CLS token，用多头注意力聚合整张图，输出 CLS 向量。替代了传统的全局平均池化。

#### (3) `ModifiedResNet`
CLIP 的 ResNet 视觉编码器（RN50/RN101 等）：
- 3 层 stem（普通 ResNet 是 1 层），用 avgpool 而非 maxpool；
- 抗混叠下采样；
- 末尾用 `AttentionPool2d` 池化。

#### (4) `LayerNorm`
兼容 fp16 的 LayerNorm：前向时先转 float32 计算，再转回原 dtype，避免半精度溢出。

#### (5) `QuickGELU`
GELU 的快速近似：`x * sigmoid(1.702 * x)`。

#### (6) `ResidualAttentionBlock`
Transformer 残差注意力块（Pre-LN）：
```
x = x + MultiheadAttention(LayerNorm(x))   # 自注意力 + 残差
x = x + MLP(LayerNorm(x))                  # FFN + 残差
```
MLP 为 `Linear(d → 4d) → QuickGELU → Linear(4d → d)`。

#### (7) `Transformer`
`ResidualAttentionBlock` 的堆叠。

#### (8) `VisionTransformer`（本项目实际使用的视觉编码器）
ViT 流程：
1. `conv1`（kernel=stride=patch_size）把图像切成 patch 并映射到 `width` 维；ViT-B/32 中 224×224×3 → 7×7×768；
2. 展平为 token，前插 CLS token，加位置编码；
3. `LayerNorm → Transformer → 取 CLS token → LayerNorm → 线性投影到 512 维`。

> 项目里通过 forward hook 抓取 `visual.transformer` 的输入（即 `ln_pre` 之后的完整 token 序列），
> 去掉 CLS 后得到 `token_img_patch`（ViT patch token，`[B, 49, 768]`），作为额外特征送入后续 Transformer。

#### (9) `CLIP`
完整双塔模型，包含：
- `encode_image(image)`：图像 → 512 维语义向量；
- `encode_text(text)`：token 序列 → 512 维语义向量（取 EOT token 位置的特征）；
- `forward(image, text)`：归一化后算余弦相似度 logits（用于对比学习）。

#### (10) `build_model(state_dict)`
从预训练权重推断超参（是 ViT 还是 ResNet、层数、宽度、分辨率等）并构建模型。

### 3.2 在本项目中的角色

CLIP 以 **冻结（不参与训练）** 的方式使用，提供两个能力：

| 能力 | 方法 | 输出 |
|------|------|------|
| 图像语义编码 | `encode_image` | `img_feats [B, 512]` |
| 文本语义编码 | `encode_text` | 属性文本向量 `[N_text, 512]` |

其中 `logit_scale`（可学习温度系数，初始约 `log(1/0.07)`）被复用于计算相似度。

---

## 4. models.py —— CLIP 语义融合特征提取

该文件定义了两个类，`GEWithCLIPModel` 与 `GEWithCLIPModel_zhao`，二者核心逻辑相同，仅 CNN 分支不同。

### 4.1 文本提示（prompt）设计

模型预先定义 4 组文本提示，用 CLIP 文本编码器离线编码：

| 属性组 | 文本提示 | 数量 | 含义 |
|--------|----------|------|------|
| 光照 illumination | "a face with bright light" / "low light" / "shadows" | 3 | 亮光/弱光/阴影 |
| 头姿 headpose | "a frontal face" / "a profile face" | 2 | 正面/侧面 |
| 背景 background | "a face on bright background" / "dark background" | 2 | 亮/暗背景 |
| 视线方向 label | "A photo of a face looking left / upper left / up / ..." | 8 | 8 个方向 |

> 光照/头姿/背景被视为与 gaze 无关的"干扰上下文"；视线方向文本是与任务直接相关的"标签"。

### 4.2 前向计算步骤

1. **图像编码**：`img_feats = encode_image(face)` → `[B, 512]`；
2. **方向标签编码**：`label_feats = encode_text(label_tokens)` → `[8, 512]`；
3. **相似度计算**：归一化后，`sim = logit_scale.exp() * img_norm @ text_norm^T`；
4. **属性选择（argmax）**：选出每个样本相似度最高的光照/头姿/背景/方向文本；
5. **特征构造**：
   - `feature_1 = img_feats + 光照 + 头姿 + 背景`，再 L2 归一化 → **上下文补偿特征**；
   - `feature_2 = img_feats + 视线方向标签`，再 L2 归一化 → **任务对齐特征**；
6. **CNN 特征**：
   - `GEWithCLIPModel`：`main_model` 输出全局向量（fc 前）→ `feature_3 [B, C]`；
   - `GEWithCLIPModel_zhao`：`create_feature_extractor` 提取 `layer4` 特征图 → `feature_3 [B, C, H, W]`；
7. **融合**：拼接 `[feature_1, feature_2, feature_3]` 送入 MLP `fused_dim→256→3`，回归 3D gaze。

### 4.3 两个类的区别

| 类 | CNN 分支 | feature_3 形状 | 用途 |
|----|----------|----------------|------|
| `GEWithCLIPModel` | ResNet-50/18/EdgeNeXt 去掉分类头 | `[B, C]` 全局向量 | 简单 MLP 融合方案 |
| `GEWithCLIPModel_zhao` | `create_feature_extractor` 取 `layer4` 特征图 | `[B, C, H, W]` 特征图 | 供后续切成 patch token 送入 Transformer |

> 实际训练（`train.py`）中，使用 `GEWithCLIPModel_zhao` 生成中间特征（含特征图），
> 最终 gaze 回归由 `transformer_models.py` 的 `TransformerDeepSeek_gaze` 完成，
> 而不是用 `models.py` 里的 `fuse_model` MLP。

---

## 5. transformer_models.py —— MoE Transformer

该文件实现了一系列 Transformer / MoE 组件，顶层是 `TransformerDeepSeek_gaze`。整体参考了 DeepSeek-V3 的 MoE 结构。

### 5.1 基础组件

#### (1) `RMSNorm`
均方根归一化（LLaMA/DeepSeek 风格）：
```
x / sqrt(mean(x^2) + eps) * weight
```
不做均值中心化，计算更快。

#### (2) `Attention`
对 `nn.MultiheadAttention` 的封装（`batch_first=True`）。

#### (3) `FlashAttention`
基于 `flash_attn` 库的高效注意力封装，支持自注意力/交叉注意力、残差返回。

#### (4) `FeedForward`
标准 FFN：`Linear(d→inter) → SiLU → Linear(inter→d)`。

#### (5) `Router` / `MixtureOfExperts`
简单 MoE 的早期实现：线性门控 + softmax + top-k 专家加权求和。
> ⚠️ 该 `MixtureOfExperts.forward` 中引用了未定义变量（`j`、`sample_indices`），
> 是历史遗留/未完成代码，**实际项目未使用**，实际使用的是下面的 `MoE`。

#### (6) `Gate`（DeepSeek 风格门控）
对每个 token：
1. `scores = x @ weight^T`；
2. softmax（减 max 稳定数值）或 sigmoid；
3. 可选分组选 top-k 组；
4. `topk` 选专家索引，从原始打分 `gather` 权重；
5. `clamp(min=1e-7)`、sigmoid 归一化、乘 `route_scale`。

返回 `(weights, indices)`，形状均为 `[N, topk]`。

#### (7) `Expert`（SwiGLU 专家）
```
output = w2( SiLU(w1(x)) * w3(x) )
```

#### (8) `MoE`（实际使用的 MoE 模块）
组成：`Gate` + 路由专家 `ModuleList` + 共享专家 `FeedForward`。
前向：
1. 展平输入为 `[N, dim]`；
2. `gate` 得到 `weights [N,topk]`、`indices [N,topk]`；
3. 对每个专家用 mask 找出选中它的样本，`index_add_` 累加加权输出；
4. 加上共享专家输出，恢复形状。

#### (9) `PositionalEncoding`
正弦位置编码（sin/cos）。

### 5.2 组合组件

#### `TransformerBlock`
自注意力 + 交叉注意力 + 简单 MoE，各带残差和 RMSNorm。

#### `Block`（DeepSeek 风格块）
`注意力 + (前 n_dense_layers 层用 FFN，其余层用 MoE)`，支持自注意力/交叉注意力。

#### `BlockMoba`（实际使用的块）
与 `Block` 类似，但：
- 前 `n_dense_layers = 9` 层用 FFN，其余用 MoE；
- 注意力在 `_moba_attention` 中实现为**标准缩放点积多头注意力**（命名源自 MoBA，但并未实现真正的稀疏块注意力）。

结构：
```
x_norm = RMSNorm(x)
attn   = Self/CrossAttention(x_norm)
out    = x + attn
out    = out + (MoE/FF)(RMSNorm(out))
```

### 5.3 顶层模型 `TransformerDeepSeek_gaze`

**输入**（`raw_inputs` 字典）：

| key | 形状 | 来源 | 说明 |
|-----|------|------|------|
| `feature_1` | `[B, 512]` | CLIP | 上下文补偿特征 |
| `feature_2` | `[B, 512]` | CLIP | 任务对齐特征 |
| `feature_3` | `[B, N, 2048]` | CNN layer4 展平 | 视觉几何 token |
| `token_img_patch` | `[B, N_patch, 768]` | ViT hook | 图像 patch token（可选） |

**投影层**：
- `proj_f1`: 512 → 768
- `proj_f2`: 512 → 768
- `proj_f3`: 2048 → 768
- `proj_patch`: 768 → 768

**前向流程**：
1. 各特征投影到统一 `d_model=768` 维，构造 token：
   - `feature_1/2` 各 1 个 token（`unsqueeze(1)`）；
   - `feature_3` 为 token 序列；
   - `token_img_patch`（可选）为 patch token 序列；
2. 拼接 `CLS token + 全部 token` → `[B, L, 768]`；
3. 经过 `num_layers` 层 `BlockMoba`（每层后加 Dropout）；
4. 取 CLS token → `linear_head` 回归输出 `[B, 3]` 的 3D gaze 向量。

> 训练时 `train.py` 中的配置为：`num_layers=12, embed_dim=512, inter_dim=2048, num_heads=8, n_routed_experts=4, n_activated_experts=2, n_shared_experts=2, moe_inter_dim=1024, d_model=768, out_dim=3`。

---

## 6. 训练流程串联（train.py）

1. **构建 CLIP 融合模型** `GEWithCLIPModel`（即 `GEWithCLIPModel_zhao`），并冻结其 CLIP 视觉/文本编码器参数；
2. **构建 Transformer** `TransformerDeepSeek_gaze`（`d_model=768, out_dim=3`）；
3. **`process_batch`** 负责从 batch 中生成 `raw_inputs`：
   - 用 forward hook 抓取 ViT 完整 token，去掉 CLS 得到 `token_img_patch`；
   - 计算 `feature_1/2`（CLIP 语义融合）；
   - 用 CNN 提取 `layer4` 特征图并展平成 token 作为 `feature_3`；
   - 按 `ABLA_CONFIG` 消融开关控制各特征是否置零；
4. **损失函数**：
   - `angular_loss`：预测与真值 3D 向量的夹角（弧度）；
   - `feature_separation_loss`：让 `feature_1`（无关上下文）与 `feature_2`（任务相关）尽量正交/分离；
   - `loss = angular_loss + λ * separation_loss`；
5. **优化**：AdamW + 余弦退火，AMP 混合精度（`autocast` + `GradScaler`）；
6. 每轮在测试集计算 **Mean Angular Error（度）** 作为评估指标，保存最优模型。

---

## 7. 张量形状速查表

| 名称 | 形状 | 说明 |
|------|------|------|
| 输入 `face` | `[B, 3, 224, 224]` | CLIP 预处理后的人脸 |
| 输入 `other_face` | `[B, 3, 224, 224]` | CNN 预处理后的人脸 |
| `img_feats` | `[B, 512]` | CLIP 图像语义向量 |
| `illum_feats` | `[3, 512]` | 光照文本特征 |
| `head_feats` | `[2, 512]` | 头姿文本特征 |
| `bg_feats` | `[2, 512]` | 背景文本特征 |
| `label_feats` | `[8, 512]` | 视线方向文本特征 |
| `feature_1` | `[B, 512]` | 图像 + 光照/头姿/背景（归一化） |
| `feature_2` | `[B, 512]` | 图像 + 视线方向标签（归一化） |
| CNN 特征图 | `[B, 2048, H, W]` | ResNet-50 layer4 |
| `feature_3` | `[B, H*W, 2048]` | 特征图展平为 token |
| `token_img_patch` | `[B, 49, 768]` | ViT patch token（224/32=7 → 49） |
| `token_f1/f2` | `[B, 1, 768]` | feature_1/2 投影后 |
| `token_f3` | `[B, H*W, 768]` | feature_3 投影后 |
| transformer 输入 | `[B, 1 + 1 + 1 + N_f3 + N_patch, 768]` | CLS + 全部 token |
| 输出 gaze | `[B, 3]` | 3D 视线方向向量 |

---

## 8. 关键设计思想总结

1. **CLIP 语义先验**：用文本描述"光照/头姿/背景/方向"等属性，通过冻结的 CLIP 把不可见的上下文因素显式建模，缓解 gaze 估计中的外观变化干扰。

2. **特征解耦与融合**：
   - `feature_1` 携带与 gaze 无关的上下文（光照/头姿/背景），用于"补偿"环境变化；
   - `feature_2` 携带与任务直接相关的方向语义；
   - 通过 `feature_separation_loss` 鼓励二者正交，实现**任务相关/无关特征的分离**。

3. **多源 token 聚合**：把 CLIP 语义（全局）、CNN 特征图（局部空间）、ViT patch（细粒度）统一投影后作为 token 输入 Transformer，信息互补。

4. **MoE 提升容量**：DeepSeek 风格 MoE（gate + 路由专家 + 共享专家）在保持计算量可控的同时增大模型容量；前若干层用 dense FFN 保证基础表示，后续层用 MoE 提升表达能力。

5. **CLS 聚合 + 回归头**：借鉴 ViT，拼接可学习 CLS token，最后取 CLS 输出经线性头回归 3D gaze，简洁有效。

6. **可消融设计**：`ABLA_CONFIG` 支持对 `feature_1~feature_4` 单独开关（置零），方便验证各特征流的贡献。

---

## 附：文件间引用关系

```
config.py  ──►  clip.load("ViT-B/32") ──►  clipmodel.py  (CLIP 结构)
                                             │
                                             ▼
models.py  ──►  GEWithCLIPModel(_zhao)  使用 CLIP + CNN 生成 feature_1/2/3
                                             │
                                             ▼
transformer_models.py  ──►  TransformerDeepSeek_gaze  聚合 token，回归 3D gaze
                                             ▲
                                             │
train.py  ──►  数据加载 + process_batch + 训练/测试循环
```

> 注：`train.py` 中 `from model_zhao_test import TransformerDeepSeek_gaze` 指向的即本仓库的
> `transformer_models.py`（若导入报错，可将该行改为 `from transformer_models import TransformerDeepSeek_gaze`）。
